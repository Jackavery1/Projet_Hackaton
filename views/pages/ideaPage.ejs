<!DOCTYPE html>
<html lang="fr">

<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>
<%= idea.titre %>
</title>
<link rel="stylesheet" type="text/css" href="../styles.css">
</head>

<body class="size-full">

<%- include('../partials/header'); %>

<main class="relative w-full flex flex-col justify-start mr-5">

<div class="flex justify-center items-center mb-5">

<form action="/api/idees/<%= idea._id %>?_method=DELETE" method="POST"></form>
<button type="submit" aria-label="Supprimer l'idée : <%= idea.titre %>"
class="bg-transparent hover:bg-amber-500 rounded-full text-white font-bold py-[0.2rem] px-[0.8rem] text-xl mr-8">
X - Delete
</button>
</form>

<h1 class="text-center text-4xl font-black">
<%= idea.titre %>
</h1>

<form action=<%="/api/idees/" + idea._id + "/" + (idea.like ? "deletelike" : "like" ) %> method="post"
id="idea_like">
<button type="submit" aria-label="<%= idea.like ? " Retirer votre j'aime pour l'idée"
: "J'aime cette idée" %>"
class="bg-transparent hover:bg-amber-500 rounded-full text-white font-bold py-[0.2rem]
px-[0.6rem] text-3xl ml-8">
<%= idea.like ? "♥" : "♡" %>
</button>
</form>

</div>

<h2 class="text-center text-xl font-bold mb-5" aria-label="Categorie : <%= idea.category %>">
<%= idea.category %>
</h2>
<hr class="mb-5">

<div class="w-full max-w-5xl mx-auto flex flex-col mt-5 mb-12 p-4 rounded-xl bg-orange-500">

<p class="text-lg text-black font-bold">
<%= idea.description %>
</p>

<hr class="mt-3">
<p class="text-sm text-slate-900 font-medium mt-2">Likes : <%= idea.likes %> - Commentaires : <%=
idea.commentaires.length %>
</p>

</div>
<hr class="mb-5">

<%- include('../partials/formBox', {form_type: "comment" , idea: idea }); %>

<ul aria-label="Liste des commentaires pour l'idée : <%= idea.titre %>"
class="w-full max-w-5xl mx-auto flex flex-col mt-5 mb-12 p-4">
<%- idea.commentaires.map((comment)=> ( include('../partials/componentComment', {comment: comment, idea: idea}) )) %>
</ul>

</main>

</body>

<script src="/js/app.js"></script>
<script src="/js/ideaPage.js"></script>

</html>
